# M4 显示方案与数据服务

- [x] 将桌面外壳收敛为单层一级侧栏。
- [x] 把显示方案分类改为横向标签，方案库改为响应式两列卡片。
- [x] 将右栏升级为固定设备控制台，刷新时保留上一张有效帧。
- [x] 新增数据服务页并绑定共享数据状态，不复制读取逻辑。
- [x] 移除旧版第三方额度数据源、登录窗口、解析器和唯一使用的嵌入式浏览器依赖。
- [x] 检查 1280 × 800 和 1080 × 680 布局、滚动与键盘焦点。
