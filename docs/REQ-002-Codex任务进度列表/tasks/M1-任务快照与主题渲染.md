# M1 任务快照与主题渲染

- [x] 新增最小化的任务快照模型，只保存标题、状态、更新时间与可选真实步骤数。
- [x] 使用本机 `codex app-server --stdio` 的 `initialize`、`initialized`、`thread/list` 与只读 `thread/turns/list` 读取任务，不调用任何控制方法。
- [x] 实现首条优先级和最近更新时间排序，不使用线程 ID、预览、路径或对话正文作为显示内容。
- [x] 将 `ai-quota` 的底部信息卡替换为两条紧凑任务卡，保留非 Codex 数据源的原有剩余信息。
- [x] 增加 `codex-tasks` 四行独立主题，任务不足时使用真实空状态占位。
