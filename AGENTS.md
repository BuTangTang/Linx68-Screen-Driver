# Linx68 Screen Driver Agent Instructions

- 使用中文与用户沟通，代码标识符、命令和日志保持原文。
- 修改 Codex 额度或任务、刷新与缓存、音乐或网易云歌词、天气或定位、设备预览或推送，以及相关需求和测试前，必须完整读取 `skills/linx68-realtime-data-quality/SKILL.md`。
- 把加载态、成功态、空数据、陈旧数据、错误态和恢复过程作为同一功能的一部分设计与验收。
- 不能以编译通过代替核心 smoke、UI smoke、真实数据源和设备帧验证。
- 保留用户已有的无关改动；未经用户当轮明确要求，不执行 `git commit` 或 `git push`。
